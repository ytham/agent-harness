# Copy to .env and fill in your keys.
# The harness reads these automatically when loaded.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# Set to 1 to enable live integration tests (requires real API keys)
HARNESS_LIVE_TESTS=
